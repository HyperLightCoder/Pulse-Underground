<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Pulse Underground - Decentralized Messaging</title>
    <link rel="icon" type="image/jpeg" href="image1.jpeg">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/ethers/5.7.2/ethers.umd.min.js"></script>
    <script src="script.js"></script>
    <style>
        :root {
            --primary: #7c4dff;
            --primary-light: #b388ff;
            --bg-dark: #0d0b14;
            --bg-light: #13101c;
            --bg-lighter: #1a1327;
            --border: #2a2438;
            --text: #fff;
            --neon-glow: 0 0 5px var(--primary), 0 0 10px var(--primary), 0 0 15px var(--primary);
            --success: #4CAF50;
            --error: #f44336;
            --warning: #FF9800;
            --info: #2196F3;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: var(--bg-dark);
            color: var(--text);
            line-height: 1.5;
            font-size: 16px;
            min-height: 100vh;
            position: relative;
            overflow-x: hidden;
        }

        /* Loading animation */
        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 2px solid var(--bg-lighter);
            border-top: 2px solid var(--primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Button system */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 12px 24px;
            background: linear-gradient(45deg, var(--primary), var(--primary-light));
            color: var(--text);
            border: none;
            border-radius: 12px;
            font-weight: 600;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            text-decoration: none;
            min-height: 48px;
            min-width: 120px;
            gap: 8px;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
            transition: left 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn:hover {
            transform: translateY(-2px) scale(1.02);
            box-shadow: 0 8px 32px rgba(124, 77, 255, 0.4), 0 0 20px rgba(124, 77, 255, 0.3);
            background: linear-gradient(45deg, #6a3fff, #a370ff);
        }

        .btn:active {
            transform: translateY(0) scale(0.98);
            box-shadow: 0 4px 20px rgba(124, 77, 255, 0.3);
        }

        .btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .btn.primary {
            background: linear-gradient(45deg, var(--primary), var(--primary-light));
        }

        .btn.primary:hover {
            background: linear-gradient(45deg, #6a3fff, #a370ff);
        }

        .btn.secondary {
            background: linear-gradient(45deg, #666, #444);
            color: #ccc;
        }

        .btn.secondary:hover {
            background: linear-gradient(45deg, #555, #333);
        }

        .btn.success {
            background: linear-gradient(45deg, var(--success), #5CBF50);
        }

        .btn.success:hover {
            background: linear-gradient(45deg, #45a049, #60c760);
        }

        .btn.error {
            background: linear-gradient(45deg, var(--error), #d32f2f);
        }

        .btn.error:hover {
            background: linear-gradient(45deg, #c62828, #e53935);
        }

        .btn.warning {
            background: linear-gradient(45deg, var(--warning), #F57C00);
        }

        .btn.warning:hover {
            background: linear-gradient(45deg, #E65100, #FF6F00);
        }

        .btn.info {
            background: linear-gradient(45deg, var(--info), #1976D2);
        }

        .btn.info:hover {
            background: linear-gradient(45deg, #1565C0, #1E88E5);
        }

        .btn.small {
            padding: 8px 16px;
            font-size: 12px;
            min-height: 32px;
            min-width: 80px;
        }

        .btn.icon {
            padding: 8px;
            min-width: auto;
            border-radius: 8px;
        }

        /* Main layout */
        #app-container {
            display: flex;
            min-height: 100vh;
            max-width: 1280px;
            margin: 0 auto;
            background: var(--bg-light);
            border: 1px solid var(--border);
        }

        /* Sidebar */
        #sidebar-left,
        #sidebar-right {
            position: sticky;
            top: 0;
            height: 100vh;
            background: var(--bg-light);
            padding: 20px;
            overflow-y: auto;
            border-right: 1px solid var(--border);
            border-left: 1px solid var(--border);
        }

        #sidebar-left {
            width: 280px;
            border-right: 1px solid var(--border);
        }

        #sidebar-right {
            width: 320px;
            border-left: 1px solid var(--border);
        }

        /* Main content */
        main {
            flex-grow: 1;
            width: calc(1280px - 600px);
            background: var(--bg-light);
            padding: 20px;
        }

        /* Header */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px solid var(--border);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            color: var(--primary-light);
            font-size: 20px;
            font-weight: 700;
            text-decoration: none;
        }

        .logo img {
            width: 40px;
            height: 40px;
            border-radius: 8px;
        }

        .header-actions {
            display: flex;
            gap: 12px;
        }

        /* Connection status */
        .connection-status {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 4px 12px;
            border-radius: 6px;
            font-size: 12px;
            font-weight: 500;
        }

        .connection-status.connected {
            background: rgba(76, 175, 80, 0.2);
            color: var(--success);
            border: 1px solid var(--success);
        }

        .connection-status.disconnected {
            background: rgba(244, 67, 54, 0.2);
            color: var(--error);
            border: 1px solid var(--error);
        }

        .connection-status.connecting {
            background: rgba(255, 152, 0, 0.2);
            color: var(--warning);
            border: 1px solid var(--warning);
        }

        /* Channel navigation */
        .channel-nav {
            margin-bottom: 30px;
        }

        .channel-nav h3 {
            color: var(--primary-light);
            margin-bottom: 15px;
            font-size: 16px;
            font-weight: 600;
        }

        .channel-list {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .channel-btn {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 16px;
            background: var(--bg-lighter);
            color: var(--text);
            border: 1px solid var(--border);
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.2s;
            cursor: pointer;
        }

        .channel-btn:hover {
            background: var(--bg-light);
            border-color: var(--primary);
            transform: translateX(4px);
        }

        .channel-btn.active {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
        }

        .channel-btn.active:hover {
            background: #6a3fff;
            transform: translateX(0);
        }

        .channel-btn .channel-icon {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background: var(--primary);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 10px;
            font-weight: bold;
        }

        /* Message input */
        .message-input-container {
            margin-bottom: 20px;
            position: relative;
        }

        #message-input {
            width: 100%;
            padding: 16px 20px;
            background: var(--bg-lighter);
            color: var(--text);
            border: 1px solid var(--border);
            border-radius: 12px;
            font-size: 16px;
            resize: none;
            min-height: 100px;
            transition: all 0.2s;
        }

        #message-input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 2px rgba(124, 77, 255, 0.2);
        }

        #message-input::placeholder {
            color: #888;
        }

        /* Message container */
        #messages-container {
            flex: 1;
            overflow-y: auto;
            padding: 20px;
            background: var(--bg-dark);
            border-radius: 12px;
            margin-bottom: 20px;
        }

        .message {
            background: var(--bg-lighter);
            border-radius: 12px;
            padding: 16px;
            margin-bottom: 16px;
            border-left: 4px solid var(--primary);
            transition: all 0.2s;
        }

        .message:hover {
            background: var(--bg-light);
            transform: translateX(4px);
        }

        .message-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
            font-size: 14px;
            color: #888;
        }

        .message-address {
            font-weight: 600;
            color: var(--primary-light);
        }

        .message-time {
            color: #666;
        }

        .message-content {
            line-height: 1.5;
            word-wrap: break-word;
        }

        .no-messages {
            text-align: center;
            color: #888;
            padding: 40px;
            font-style: italic;
        }

        /* Search */
        .search-container {
            margin-bottom: 20px;
            display: flex;
            gap: 8px;
        }

        #search-input {
            flex: 1;
            padding: 12px 16px;
            background: var(--bg-lighter);
            color: var(--text);
            border: 1px solid var(--border);
            border-radius: 8px;
            font-size: 14px;
        }

        #search-input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 2px rgba(124, 77, 255, 0.2);
        }

        /* Profile */
        .profile-section {
            margin-bottom: 30px;
        }

        .profile-section h3 {
            color: var(--primary-light);
            margin-bottom: 15px;
            font-size: 16px;
            font-weight: 600;
        }

        .profile-form {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 4px;
        }

        .form-group label {
            color: #888;
            font-size: 12px;
            font-weight: 500;
        }

        .form-group input {
            padding: 10px 12px;
            background: var(--bg-lighter);
            color: var(--text);
            border: 1px solid var(--border);
            border-radius: 8px;
            font-size: 14px;
        }

        .form-group input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 2px rgba(124, 77, 255, 0.2);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 20px;
            color: #666;
            font-size: 12px;
        }

        footer a {
            color: var(--primary);
            text-decoration: none;
        }

        footer a:hover {
            text-decoration: underline;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .fade-in {
            animation: fadeIn 0.3s ease-out;
        }

        /* Responsive */
        @media (max-width: 768px) {
            #app-container {
                flex-direction: column;
            }

            #sidebar-left,
            #sidebar-right {
                width: 100%;
                position: relative;
                height: auto;
            }

            #sidebar-right {
                order: -1;
            }

            main {
                width: 100%;
            }

            .header-actions {
                flex-direction: column;
                gap: 8px;
            }

            .channel-btn {
                padding: 10px 12px;
                font-size: 13px;
            }
        }

        /* Notification system */
        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            padding: 12px 20px;
            border-radius: 8px;
            color: white;
            z-index: 10000;
            animation: slideIn 0.3s ease-out;
            max-width: 300px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.2);
        }

        @keyframes slideIn {
            from {
                transform: translateX(100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        .notification-success {
            background: #4CAF50;
        }

        .notification-error {
            background: #f44336;
        }

        .notification-warning {
            background: #FF9800;
        }

        .notification-info {
            background: #2196F3;
        }

        /* Loading states */
        .loading-container {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px;
        }

        .skeleton {
            background: var(--bg-lighter);
            border-radius: 8px;
            animation: pulse 1.5s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        /* New message indicator */
        .new-messages-indicator {
            position: fixed;
            top: 50%;
            right: 20px;
            transform: translateY(-50%);
            background: var(--primary);
            color: white;
            padding: 8px 16px;
            border-radius: 24px;
            cursor: pointer;
            box-shadow: 0 4px 20px rgba(124, 77, 255, 0.4);
            z-index: 1000;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { box-shadow: 0 0 0 0 rgba(124, 77, 255, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(124, 77, 255, 0);
        }
    </style>
</head>
<body>
    <div id="app-container">
        <!-- Sidebar Left -->
        <aside id="sidebar-left">
            <div class="logo">
                <img src="image1.jpeg" alt="Pulse Underground">
                Pulse Underground
            </div>

            <!-- Connection Status -->
            <div class="connection-status" id="connection-status">
                Connect Wallet
            </div>

            <!-- Channel Navigation -->
            <div class="channel-nav">
                <h3>Channels</h3>
                <div class="channel-list">
                    <a href="#" class="channel-btn active" data-channel="main">
                        <span class="channel-icon">M</span>
                        Main Channel
                    </a>
                    <a href="#" class="channel-btn" data-channel="self">
                        <span class="channel-icon">S</span>
                        Self Channel
                    </a>
                    <a href="#" class="channel-btn" data-channel="custom">
                        <span class="channel-icon">C</span>
                        Custom Channel
                    </a>
                </div>
            </div>

            <!-- Profile Section -->
            <div class="profile-section">
                <h3>Profile</h3>
                <div class="profile-form">
                    <div class="form-group">
                        <label for="username">Username</label>
                        <input type="text" id="username" placeholder="Enter your username">
                    </div>
                    <div class="form-group">
                        <label for="avatar">Avatar URL</label>
                        <input type="text" id="avatar" placeholder="Enter avatar image URL">
                    </div>
                    <button class="btn small primary" id="save-profile">Save Profile</button>
                </div>
            </div>
        </aside>

        <!-- Main Content -->
        <main>
            <!-- Header -->
            <header>
                <a href="#" class="logo">
                    <img src="image1.jpeg" alt="Pulse Underground" width="32" height="32">
                    Pulse Underground
                </a>
                <div class="header-actions">
                    <button class="btn small secondary" id="search-btn">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <circle cx="11" cy="11" r="8"></circle>
                            <path d="m21 21-4.35-4.35"></path>
                        </svg>
                        Search
                    </button>
                    <button class="btn primary" id="post-btn">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
                            <polyline points="14 2 14 8 20 8"></polyline>
                            <line x1="16" y1="13" x2="8" y2="13"></line>
                            <line x1="16" y1="17" x2="8" y2="17"></line>
                            <polyline points="10 9 9 9 8 9"></polyline>
                        </svg>
                        Post Message
                    </button>
                </div>
            </header>

            <!-- Search Container -->
            <div class="search-container" id="search-container" style="display: none;">
                <input type="text" id="search-input" placeholder="Search messages...">
                <button class="btn small secondary" id="search-cancel">Cancel</button>
            </div>

            <!-- Messages Container -->
            <div id="messages-container">
                <div class="loading-container">
                    <div class="skeleton" style="width: 80%; height: 100px; margin: 0 auto 20px;"></div>
                    <div class="skeleton" style="width: 60%; height: 100px; margin: 0 auto 20px;"></div>
                    <div class="skeleton" style="width: 70%; height: 100px; margin: 0 auto;"></div>
                </div>
            </div>

            <!-- Message Input -->
            <div class="message-input-container">
                <textarea id="message-input" placeholder="What's happening? Type your message here..."></textarea>
            </div>
        </main>

        <!-- Sidebar Right -->
        <aside id="sidebar-right">
            <div class="channel-nav">
                <h3>Active Users</h3>
                <div class="channel-list">
                    <div class="channel-btn">
                        <span class="channel-icon" style="background: #4CAF50;">U</span>
                        User1234
                    </div>
                    <div class="channel-btn">
                        <span class="channel-icon" style="background: #FF9800;">U</span>
                        CryptoNinja
                    </div>
                    <div class="channel-btn">
                        <span class="channel-icon" style="background: #2196F3;">U</span>
                        BlockchainDev
                    </div>
                </div>
            </div>

            <div class="profile-section">
                <h3>Stats</h3>
                <div class="channel-list">
                    <div class="channel-btn">
                        <span class="channel-icon">M</span>
                        Messages: 42
                    </div>
                    <div class="channel-btn">
                        <span class="channel-icon">U</span>
                        Users: 128
                    </div>
                    <div class="channel-btn">
                        <span class="channel-icon">T</span>
                        Transactions: 256
                    </div>
                </div>
            </div>
        </aside>
    </div>

    <!-- Footer -->
    <footer>
        <p>&copy; 2026 Pulse Underground. Built on PulseChain. <a href="#">Privacy Policy</a> | <a href="#">Terms</a></p>
    </footer>

    <script>
        // Initialize the Pulse Underground app
        document.addEventListener('DOMContentLoaded', function() {
            if (window.PulseUnderground) {
                new window.PulseUnderground();
            }
        });
    </script>
</body>
</html>
